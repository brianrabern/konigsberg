# BrooksListForm

## Book
§Brooks' theorem for list coloring (under Kernel magic) — section stub in the
TeX. We state the standard Erdős–Rubin–Taylor characterization.

## Distinct from EXTERNAL BrooksLean
`BrooksLean` (`external-verified`) is ordinary chromatic Brooks. This entry is
the in-tree *list* form (`DegreeChoosable` ↔ ¬ Gallai tree).

## Fidelity review
Gallai tree = connected + every block is complete or odd cycle (`IsGallaiTree`).
`DegreeChoosable` = `FChoosable` at `f = degree` (Areas/Basic). Connected
hypothesis matches the classical statement.
