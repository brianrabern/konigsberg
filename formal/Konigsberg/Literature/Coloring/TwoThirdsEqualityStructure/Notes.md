# TwoThirdsEqualityStructure

## Book
`gct.tex` `\label{TwoThirdsEqualityStructure}`: under ω ≥ (2/3)(Δ+1) and
X_𝒬 connected, either ⋂𝒬 ≠ ∅ or Δ(X_𝒬) ≤ 2 with disjoint neighbours of A and
|A∩B| = |A∩C| = ω/2.

## Fidelity review
Non-strict ω inequality as `3ω ≥ 2(Δ+1)`. Geometry conjuncts match the book
bullet. Uses ℕ division `cliqueNum / 2` (book assumes even ω in this regime).
