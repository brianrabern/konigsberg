# Agent evals

Task benchmark suite, present from day one (per plan §8). Include a brutally
honest baseline: human-with-tools vs agent-with-tools, so the agent loop's
MARGINAL value is measured, not assumed. The library and empirical tools have
obvious standalone value; the loop is the part most at risk of underdelivering.
